{
  "type": "module",
  "main": "lib/index.ts",
  "scripts": {
    "build": "rm -rf dist/ && deno-node-build --root . --out dist/ --entry lib/index.ts",
    "test": "c8 --reporter lcov --reporter html --reporter text --exclude lib/snippets mocha --expose-gc test/**/*.ts",
    "lint": "eslint .",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@k13engineering/eslint-rules": "^0.0.6",
    "@k13engineering/releasetool": "^0.0.5",
    "@types/mocha": "^10.0.10",
    "@types/node": "^25.0.3",
    "c8": "^10.1.3",
    "deno-node": "^0.0.12",
    "mocha": "^11.7.5",
    "node-archibald": "^0.0.7",
    "typescript": "^6.0.3"
  }
}
